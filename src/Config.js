const config = {
    APP_ENV: process.env.REACT_APP_ENV,
    API_URL: process.env.REACT_APP_API_URL || "http://localhost:5000",
    FRONTEND_URL: process.env.REACT_APP_APP_URL,
};

export default config;
